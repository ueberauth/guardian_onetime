language: elixir
dist: trusty
addons:
  postgresql: "9.6"
elixir:
  - 1.5
  - 1.4

otp_release:
  - 20.0
  - 19.3

services:
  - postgresql

env:
  global:
    - MIX_ENV=test
    - POSTGRES_USER="postgres"
    - POSTGRES_PASS=""

notifications:
  email: false

sudo: true

script:
  - mix test
